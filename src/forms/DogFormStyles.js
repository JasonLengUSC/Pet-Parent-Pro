import styled from "styled-components";
import { Button } from "antd";

const UploadButton = styled(Button)`
  margin-left: 1em;
`;
const ButtonStyled = styled(Button)`
  margin-right: 8px;
  margin-bottom: 2em;
`;

export { UploadButton, ButtonStyled };
